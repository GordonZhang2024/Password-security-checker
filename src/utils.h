#ifndef UTILS_H
#define UTILS_H

#include "guard.h"

std::vector<std::string> split(std::string &s, const char *splitBy);
void showProjectInfo();

#define VERSION "1.0"
#define DESCRIPTION "A simple-but-useful password Security checker written in C++"

#endif

